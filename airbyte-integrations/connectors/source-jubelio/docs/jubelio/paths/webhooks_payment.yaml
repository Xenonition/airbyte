post:
  tags:
    - Webhooks
  summary: Update Payment
  operationId: getPaymentWebhook
  description: >
    This Webhook will be called when there is a price change from the
    application.
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/webhookPayment.yaml
  responses:
    '200':
      description: Successful
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ok.yaml
