post:
  tags:
    - Webhooks
  summary: New Product
  operationId: getProductWebhook
  description: >
    This Webhook will be called when Product created or updated from
    Application.
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/webhookProduct.yaml
  responses:
    '200':
      description: Successful
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ok.yaml
